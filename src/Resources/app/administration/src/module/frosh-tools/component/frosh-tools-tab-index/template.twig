<div class="ft frosh-tab-index">
    <ft-page-head
        :title="$t('frosh-tools.tabs.index.title')"
        subtitle="Live health and performance checks for this Shopware instance."
    >
        <template #actions>
            <ft-refresh-button
                :loading="isLoading"
                label="Refresh"
                @click="refresh"
            />
        </template>
    </ft-page-head>
    <ft-panel
        title="System Health"
        :count="health ? health.length : 0"
        flush
    >
        <ft-empty
            v-if="isLoading && !health"
            loading
        />
        <ft-empty
            v-else-if="!health || health.length === 0"
            title="No health checks reported"
        />
        <div
            v-else
            class="ft-table-wrap"
        >
            <table class="ft-table">
                <thead>
                    <tr>
                        <th
                            class="ft-table__sortable"
                            :class="{ 'is-active': sortDirOf('state', 'health') }"
                            style="width: 110px;"
                            @click="toggleSort('state', 'health')"
                        >
                            Status
                            <ft-icon
                                v-if="sortDirOf('state', 'health')"
                                :name="sortDirOf('state', 'health') === 'ASC' ? 'chevron-up' : 'chevron-down'"
                            />
                        </th>
                        <th
                            class="ft-table__sortable"
                            :class="{ 'is-active': sortDirOf('snippet', 'health') }"
                            @click="toggleSort('snippet', 'health')"
                        >
                            {{ $t('frosh-tools.name') }}
                            <ft-icon
                                v-if="sortDirOf('snippet', 'health')"
                                :name="sortDirOf('snippet', 'health') === 'ASC' ? 'chevron-up' : 'chevron-down'"
                            />
                        </th>
                        <th
                            class="ft-table__sortable"
                            :class="{ 'is-active': sortDirOf('current', 'health') }"
                            @click="toggleSort('current', 'health')"
                        >
                            {{ $t('frosh-tools.current') }}
                            <ft-icon
                                v-if="sortDirOf('current', 'health')"
                                :name="sortDirOf('current', 'health') === 'ASC' ? 'chevron-up' : 'chevron-down'"
                            />
                        </th>
                        <th
                            class="ft-table__sortable"
                            :class="{ 'is-active': sortDirOf('recommended', 'health') }"
                            @click="toggleSort('recommended', 'health')"
                        >
                            {{ $t('frosh-tools.recommended') }}
                            <ft-icon
                                v-if="sortDirOf('recommended', 'health')"
                                :name="sortDirOf('recommended', 'health') === 'ASC' ? 'chevron-up' : 'chevron-down'"
                            />
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <tr
                        v-for="(item, idx) in sortRows(health, 'health')"
                        :key="`h-${idx}`"
                    >
                        <td>
                            <ft-pill :variant="pillVariant(item.state)">{{ stateLabel(item.state) }}</ft-pill>
                        </td>
                        <td class="ft-table__name">
                            {{ item.snippet }}
                            <a
                                v-if="item.url"
                                class="ft-link"
                                :href="item.url"
                                target="_blank"
                                rel="noopener"
                            >
                                {{ $t('frosh-tools.tabs.index.info') }}
                            </a>
                        </td>
                        <td class="ft-mono">{{ item.current }}</td>
                        <td class="ft-mono">{{ item.recommended }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </ft-panel>
    <ft-panel
        :title="$t('frosh-tools.tabs.index.performance')"
        :count="performanceStatus ? performanceStatus.length : 0"
        :flush="!(performanceStatus && performanceStatus.length === 0)"
    >
        <ft-hero-state
            v-if="!isLoading && performanceStatus && performanceStatus.length === 0"
            variant="success"
            icon="check"
            title="All clear"
            :body="$t('frosh-tools.noRecommendations')"
        />
        <div
            class="ft-table-wrap"
            v-else-if="performanceStatus && performanceStatus.length > 0"
        >
            <table class="ft-table">
                <thead>
                    <tr>
                        <th
                            class="ft-table__sortable"
                            :class="{ 'is-active': sortDirOf('state', 'performance') }"
                            style="width: 110px;"
                            @click="toggleSort('state', 'performance')"
                        >
                            Status
                            <ft-icon
                                v-if="sortDirOf('state', 'performance')"
                                :name="sortDirOf('state', 'performance') === 'ASC' ? 'chevron-up' : 'chevron-down'"
                            />
                        </th>
                        <th
                            class="ft-table__sortable"
                            :class="{ 'is-active': sortDirOf('snippet', 'performance') }"
                            @click="toggleSort('snippet', 'performance')"
                        >
                            {{ $t('frosh-tools.name') }}
                            <ft-icon
                                v-if="sortDirOf('snippet', 'performance')"
                                :name="sortDirOf('snippet', 'performance') === 'ASC' ? 'chevron-up' : 'chevron-down'"
                            />
                        </th>
                        <th
                            class="ft-table__sortable"
                            :class="{ 'is-active': sortDirOf('current', 'performance') }"
                            @click="toggleSort('current', 'performance')"
                        >
                            {{ $t('frosh-tools.current') }}
                            <ft-icon
                                v-if="sortDirOf('current', 'performance')"
                                :name="sortDirOf('current', 'performance') === 'ASC' ? 'chevron-up' : 'chevron-down'"
                            />
                        </th>
                        <th
                            class="ft-table__sortable"
                            :class="{ 'is-active': sortDirOf('recommended', 'performance') }"
                            @click="toggleSort('recommended', 'performance')"
                        >
                            {{ $t('frosh-tools.recommended') }}
                            <ft-icon
                                v-if="sortDirOf('recommended', 'performance')"
                                :name="sortDirOf('recommended', 'performance') === 'ASC' ? 'chevron-up' : 'chevron-down'"
                            />
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <tr
                        v-for="(item, idx) in sortRows(performanceStatus, 'performance')"
                        :key="`p-${idx}`"
                    >
                        <td>
                            <ft-pill :variant="pillVariant(item.state)">{{ stateLabel(item.state) }}</ft-pill>
                        </td>
                        <td class="ft-table__name">
                            {{ item.snippet }}
                            <a
                                v-if="item.url"
                                class="ft-link"
                                :href="item.url"
                                target="_blank"
                                rel="noopener"
                            >
                                {{ $t('frosh-tools.tabs.index.info') }}
                            </a>
                        </td>
                        <td class="ft-mono">{{ item.current }}</td>
                        <td class="ft-mono">{{ item.recommended }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </ft-panel>
</div>